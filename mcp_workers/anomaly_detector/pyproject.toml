[project]
name = "datapilot-mcp-anomaly_detector"
version = "0.1.0"
description = "DataPilot MCP worker stub: anomaly_detector (Phase 0)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
