[project]
name = "datapilot-mcp-rosbag_reader"
version = "0.1.0"
description = "DataPilot MCP worker stub: rosbag_reader (Phase 0)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
