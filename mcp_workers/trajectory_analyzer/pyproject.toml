[project]
name = "datapilot-mcp-trajectory_analyzer"
version = "0.1.0"
description = "DataPilot MCP worker stub: trajectory_analyzer (Phase 0)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
