/// <reference types="vite/client" />

/**
 * Build-time constant injected by the Vite `define` in electron.vite.config.ts.
 * Holds the ISO date (YYYY-MM-DD) the renderer bundle was produced.
 */
declare const __BUILD_DATE__: string;
